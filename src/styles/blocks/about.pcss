.about__content {
    display: flex;
    flex-direction: column;
    flex: 1;
}

.about__container {
    display: flex;
    padding: 10% 16%;
}

.about__desc {
    font-weight: 600;
}

.about__title {
    font-weight: 700;
    font-size: 36px;
    color: $text-color;
    padding-top: 30px;
}

.about__ps {
    position: relative;
    padding-top: 105px;
    font-style: italic;

    &:before {
        content: url(../images/decor/star.png);
        position: absolute;
        left: -31px;
    }
}

.about__text-info {
    padding-top: 38px;
}